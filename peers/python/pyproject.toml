[project]
name = "a2a-parity-python-peers"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk[http-server]>=1.0",
    "uvicorn>=0.34",
    "starlette>=0.46",
    "httpx>=0.28",
]

[tool.ruff]
line-length = 120
